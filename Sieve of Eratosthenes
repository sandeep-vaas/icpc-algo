#include<bits/stdc++.h>
using namespace std;
//Function will print all the prime numbers less than equal to 'n';
void SieveOfEratosthenes(int n)
{
    //Initially consider all the numbers as prime number;
    bool prime[n+1];
    memset(prime,true,sizeof(prime));
    for(int i=2;i*i<=n;i++)
    {
        //if a number is prime then all numbers that is divisible by that number, mark as non-prime;
        if(prime[i])
        {
            for(int j=i*i;j<=n;j+=i)
                prime[j] = false;
        }
    }
    //Print all the prime numbers less than equal to 'n';
    for(int i=2;i<=n;i++)
        if(prime[i])
            cout << i << " ";
    cout << endl;
}
int main()
{
    int n;
    cin>>n;
    SieveOfEratosthenes(n);
    return 0;
}
